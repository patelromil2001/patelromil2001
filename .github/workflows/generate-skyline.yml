name: Generate Skyline SVG
on:
  schedule:
    - cron: '0 6 * * 0' # weekly
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Run skyline generator
        run: python scripts/generate_skyline.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/skyline.svg || true
          git commit -m "chore: update skyline.svg [skip ci]" || echo "no changes to commit"
          git push
